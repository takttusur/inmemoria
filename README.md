# InMemoria API

## Migration from MySQL

Run `docker-compose -f docker-compose.migration.yml up --build`

## Run InMemoria Backend + Frontend from develop
Run `docker-compose -f docker-compose.local.yml up --build`
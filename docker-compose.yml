services:
  takttusur.inmemoria.api:
    image: takttusur.inmemoria.api
    build:
      context: .
      dockerfile: TaktTusur.InMemoria.Api/Dockerfile
    environment:
      ConnectionStrings__InMemoriaDb: "Host=myserver;Database=mydb;Username=myuser;Password=mypassword"
